// Carregado sob demanda pelo LazyMotion (App.jsx), fora do bundle inicial.
// domMax inclui as animações de layout usadas no filtro de habilidades.
import { domMax } from 'framer-motion';

export default domMax;
